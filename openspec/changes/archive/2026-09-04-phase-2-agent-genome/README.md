# phase-2-agent-genome

Feedforward NN forward pass (numpy, decoupled from the game engine), genome representation with random init and encode/decode, plus mutate() and crossover() operators pulled forward as pure genome-to-genome logic, and a minimal Agent wrapper satisfying match.py's Chooser protocol.
